# One-time image build for the Cloud Run job. The job command is `python -m information_hunters.once`, set on the job, not here.
steps:
  - name: gcr.io/cloud-builders/docker
    args: ["build", "-f", "docker/worker.Dockerfile", "-t", "$_IMAGE", "."]
images:
  - "$_IMAGE"
